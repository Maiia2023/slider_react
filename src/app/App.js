import './styles/index.scss';
// import { usePhotos } from 'shared/hooks';
// import { Slider } from 'features';

export const App = () => {
  // const photoStrate = usePhotos();
  // photoState.getPhotos();
  return (
    <div className={'app'}>
      React Slider
      { /* <Slider slides {photoState.photos}/> */}
      { /* <Slider slides {[]}/> */}
    </div>
  );
};
